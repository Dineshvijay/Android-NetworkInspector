<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dinesh.Network">

    <uses-permission android:name="android.permission.INTERNET">
        true
    </uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CustomWebView">
        <activity android:name=".inspector.ui.ConsoleLogActivity"></activity>
        <activity android:name=".inspector.ui.NetworkInspectorDetailActivity" />
        <activity android:name=".inspector.ui.NetworkInspectActivity"></activity>
        <activity android:name=".inspector.ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>